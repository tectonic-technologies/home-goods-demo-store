# Bulk scraped source dumps — reproducible via pipeline/crawl/crawl.py, not committed
pipeline/crawl/raw/

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Shopify credentials (never commit)
pipeline/shopify/secrets.env
